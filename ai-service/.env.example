# AI Service API key (must match backend AI_SERVICE_KEY)
AI_SERVICE_KEY=internal-key

# Server port
PORT=8000

# Allowed CORS origin(s) — comma-separated (must include the frontend URL)
CLIENT_URL=http://localhost:3000
